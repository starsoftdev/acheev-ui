// @flow

import * as React from 'react';
import { LinkWithTooltip } from 'react-foundation-components/lib/global/tooltip';

import './styles.scss';

const Tooltip = (props: Object) => <LinkWithTooltip {...props} />;

export default Tooltip;
