// @flow

import * as React from 'react';
import { Tab } from 'react-foundation-components/lib/tabs';

import './styles.scss';

const TabComponent = (props: Object) => <Tab {...props} />;

export default TabComponent;
