// @flow

import * as React from 'react';
import { Tabs } from 'react-foundation-components/lib/tabs';

import './styles.scss';

const TabsComponent = (props: Object) => <Tabs {...props} />;

export default TabsComponent;
